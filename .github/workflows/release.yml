name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            binary: tsk
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            binary: tsk.exe
          - os: macos-latest
            target: x86_64-apple-darwin
            binary: tsk
          - os: macos-latest
            target: aarch64-apple-darwin
            binary: tsk

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Build
        run: cargo build --release --target ${{ matrix.target }}

      - name: List build output
        run: ls target/${{ matrix.target }}/release/

      - name: Rename binary (Unix)
        if: matrix.os != 'windows-latest'
        shell: bash
        run: mv target/${{ matrix.target }}/release/tsk tsk-${{ matrix.target }}

      - name: Rename binary (Windows)
        if: matrix.os == 'windows-latest'
        shell: bash
        run: mv target/${{ matrix.target }}/release/tsk.exe tsk-${{ matrix.target }}.exe

      - name: Upload binary
        uses: actions/upload-artifact@v4
        with:
          name: tsk-${{ matrix.target }}
          path: |
            tsk-${{ matrix.target }}
            tsk-${{ matrix.target }}.exe
          if-no-files-found: warn

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v3
        with:
          files: artifacts/**/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
